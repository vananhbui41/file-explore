<template>
    <Menu as="div" class="relative inline-block text-left">
        <MenuButton
            class="inline-flex w-full justify-center rounded-md px-4 py-2 text-sm font-medium text-gray-800 focus:outline-none focus-visible:ring-2 focus-visible:ring-white/75"
        >
            <PlusIcon class="mr-1 ml-2 h-5 w-5" aria-hidden="true" />
            Create New
        </MenuButton>

        <transition
            enter-active-class="transition duration-100 ease-out"
            enter-from-class="transform scale-95 opacity-0"
            enter-to-class="transform scale-100 opacity-100"
            leave-active-class="transition duration-75 ease-in"
            leave-from-class="transform scale-100 opacity-100"
            leave-to-class="transform scale-95 opacity-0"
        >
            <MenuItems
                class="absolute left-0 mt-2 w-48 origin-top-right divide-y divide-gray-100 rounded-md bg-white shadow-lg ring-1 ring-black/5 focus:outline-none"
            >
                <div class="px-1 py-1">
                    <MenuItem v-slot="{ active }">
                        <a
                            href="#"
                            @click.prevent="showCreateFolderModal"
                            class="text-gray-900 group flex w-full items-center rounded-md px-2 py-2 text-sm"
                        >
                            <FolderPlusIcon
                                :active="active"
                                class="mr-2 h-5 w-5 text-gray-400"
                                aria-hidden="true"
                            />
                            New Folder
                        </a>
                    </MenuItem>
                </div>
                <div class="px-1 py-1">
                    <FileUploadMenuItem />
                    <FolderUploadMenuItem />
                </div>
            </MenuItems>
        </transition>
    </Menu>
    <CreateFolderModal v-model="createFolderModal" />
</template>

<script setup>
import { Menu, MenuButton, MenuItems, MenuItem } from "@headlessui/vue";
import { PlusIcon, FolderPlusIcon } from "@heroicons/vue/20/solid";
import CreateFolderModal from "@/Components/app/CreateFolderModal.vue";
import { ref } from "vue";
import FileUploadMenuItem from "@/Components/app/FileUploadMenuItem.vue";
import FolderUploadMenuItem from "@/Components/app/FolderUploadMenuItem.vue";

const createFolderModal = ref(false);

function showCreateFolderModal() {
    createFolderModal.value = true;
}
</script>
